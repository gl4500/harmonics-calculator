# Extracted xlsm contents (binary VBA dump + sheet XMLs) — not needed in repo
_extract/

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*.log

# Local-only test caches
node_modules/

# Excel / Office lock files (created when the .xlsm is open)
~$*

# iPhone screenshots dropped into the project folder during debugging
IMG_*.png
IMG_*.jpg
IMG_*.HEIC
